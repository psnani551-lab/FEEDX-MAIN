// SOK MODEL section removed as per user request
// This component now returns null to maintain compatibility with Index.tsx

const FeaturesSection = () => {
  return null;
};

export default FeaturesSection;