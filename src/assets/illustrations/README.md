This folder is intended for site illustrations (SVGs).

Run `python3 scripts/organize_assets.py --apply` to migrate root-level `images/*.svg` into this folder and automatically update TS/TSX import paths.
